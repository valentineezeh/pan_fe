import { makeStyles } from "@material-ui/core/styles";

export const ProductSegmentStyle = makeStyles((theme) => ({
  root: {
    display: "flex",
    width: "100%",
    height: 600,
    background: "#e3e6e3",
  },
}));
